{
  "kits": {
    "TheOneKit Model Router": {
      "installedSettings": {
        "hooks": [
          "node -e const home = require('os').userInfo().homedir; process.env.HOME = home; process.env.USERPROFILE = home; require(require('path').join(home, '.claude', 'hooks', 'mr-spawn-guard.cjs'))",
          "node -e const home = require('os').userInfo().homedir; process.env.HOME = home; process.env.USERPROFILE = home; require(require('path').join(home, '.claude', 'hooks', 'mr-task-interceptor.cjs'))",
          "node -e const home = require('os').userInfo().homedir; process.env.HOME = home; process.env.USERPROFILE = home; require(require('path').join(home, '.claude', 'hooks', 'mr-inline-tracker.cjs'))",
          "node -e const home = require('os').userInfo().homedir; process.env.HOME = home; process.env.USERPROFILE = home; require(require('path').join(home, '.claude', 'hooks', 'mr-metrics.cjs'))",
          "node -e const home = require('os').userInfo().homedir; process.env.HOME = home; process.env.USERPROFILE = home; require(require('path').join(home, '.claude', 'hooks', 'mr-doctor-check.cjs'))",
          "node -e const home = require('os').userInfo().homedir; process.env.HOME = home; process.env.USERPROFILE = home; require(require('path').join(home, '.claude', 'hooks', 'mr-transparent-routing-reminder.cjs'))",
          "node -e const home = require('os').userInfo().homedir; process.env.HOME = home; process.env.USERPROFILE = home; require(require('path').join(home, '.claude', 'hooks', 'mr-telemetry.cjs'))",
          "hook:task-completed-handler.cjs",
          "hook:teammate-idle-handler.cjs"
        ],
        "mcpServers": []
      }
    },
    "TheOneKit Core": {
      "installedSettings": {
        "hooks": [
          "hook:hook-runner.cjs"
        ],
        "mcpServers": [],
        "statusLineInstalled": true,
        "envKeysInstalled": [
          "CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS"
        ]
      }
    }
  }
}