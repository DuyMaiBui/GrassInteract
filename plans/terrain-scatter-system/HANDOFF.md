# Handoff — 2026-06-03 19:10
Generated by: t1k:handoff save
Location: plans/terrain-scatter-system/HANDOFF.md

## Active Plan
- Path: `plans/terrain-scatter-system/` (plan.md + phase-1..4.md)
- Brainstorm: `plans/reports/brainstorm-terrain-scatter-system-20260603.md`
- Current phase: **Plan P1–P4 + 5 live user-requested extensions implemented.** ONE open regression (sparse grass render).
- Phase status: **blocked on a render regression** — code compiles, 5 harnesses pass, scatter byte-stable, but the demo grass renders SPARSE (render-path bug, not placement).

## Goal (delivered, minus the regression)
Generalize the grass-only system into a genre-neutral **Scatter** system: Unity-Terrain integration (height/holes/slope), multi paintable layers (grass + GPU-instanced mesh props sharing one cull pipeline), painted by a Terrain-like brush. Unity 6000.3.13f1, URP 17.3, Mono. **Not a git repo** (git:false) — changes on-disk only; per-step gate = live Unity MCP evidence on `GrassInteract@de203215` (port 6403).

## Progress

### Completed + verified (compile + harnesses + scatter byte-stable; screenshot-verified where noted)
- [x] **P1** ISurfaceSampler seam + Terrain/Raycast samplers + GrassScatter refactor + hole/slope masking. Screenshot: grass bare on steep cone flank.
- [x] **P2** GrassLayer→ScatterLayer, GrassInteractField→ScatterField (layer list), `[Obsolete]` shims, MIGRATION.md. Demo deserialized + rendered identical; 2-layer independence.
- [x] **P3** GPU mesh props — ChunkedInstanceBuffer (InstanceData==BladeInstance 20B), MeshScatterEngine (reuses GrassCull.compute UNEDITED), ScatterInstanced.shader. Screenshot: 1500 cubes rendered.
- [x] **P4 core** splat-mask, slopeRange (supersedes maxSlopeDeg), rotationOffsetEuler, randomPitch/RollRange, alignToNormal via `_OrientMode` per-instance packing (slot2 = octNormal16|pitch8|roll8 when oriented, else hash). Harness byte-stable.
- [x] **Ext1** Terrain-driven bounds — `TerrainSurfaceSampler.TerrainSizeXZ`; field rect = terrain size when bound (verified layer=20 over 64m terrain → 66). FieldBounds = fallback for non-terrain.
- [x] **Ext2** Deform toggle — `ScatterLayer.deformMode {Auto,On,Off}` (Auto→grass-on/mesh-off); ScatterInstanced.shader `_InteractsWithDeform` ports wind+interactor lean; MeshScatterEngine uploads interactors + wind globals (config defaults).
- [x] **Ext3** Painter→Inspector — `Editor/ScatterBrush.cs` (SSOT brush core, terrain-aware origin/bounds) + `Editor/ScatterFieldEditor.cs` `[CustomEditor(typeof(ScatterField),true)]` Terrain-like brush (paint/erase, size/opacity/falloff, layer dropdown, in-scene round brush). GrassPainterWindow = deprecated shim.
- [x] **Ext4** Per-instance colliders — `ScatterLayer.generateColliders + colliderMesh + colliderConvex`; MeshScatterEngine.BuildColliders spawns 1 MeshCollider GO/instance from scatter TRS (PLAY-MODE only, COLLIDER_CAP=4000 warn). Mechanism verified in edit mode.
- [x] **Ext5** TerrainScatterConfig asset + per-layer LOD — NEW `TerrainScatterConfig` SO (layers + cullCompute + indirectMaterial + mesh-wind defaults; NO terrain — asset can't ref scene). `ScatterLayer.lods[] {mesh,maxDistance}` unified LOD (grass+mesh) + LodMeshes/LodMaxDistances accessors. `GrassLODConfig` LOD fields REMOVED (keeps wind/shadows/bendHeadroom/maxBladeHeight/chunkSize). ScatterField `config` ref (null→inline fallback). Demo migrated: grass layer.lods = GrassBlade_LOD0/1/2 (18/38/1000), created `Demo/GrassInteractDemoScatterConfig.asset`, wired field.config.

### Blocked
- [ ] **Demo grass renders SPARSE** (was a dense carpet at Phase 2). Placement is identical + deterministic (ChunkBake=17469 throughout, grid 3×3 @ bounds 40). Shows on BOTH GPU and CPU tiers → a RENDER-PATH bug, not placement.

## Key Decisions Made
- Terrain binding stays on the **ScatterField component**, NOT the config asset (Unity forbids asset→scene refs). Hard constraint.
- Wind: GrassLODConfig keeps grass wind/shadows; TerrainScatterConfig wind defaults feed MESH props (replaced MESH_WIND_* constants). One wind source per consumer.
- align-to-normal + custom rotation are gated by `_OrientMode`; default-off path is byte-identical (grass keeps yaw+hash).
- Colliders: all instances (not streaming), custom MeshCollider per layer config, Play-mode only, capped.
- LOD folded into the layer (`lods[]`) for both kinds; GrassLODConfig keeps only render params.
- Sequential subagent-per-phase; main loop owns MCP verification (sub-agents stall on live gates).

## Blockers
- **Sparse grass render regression.** Root cause NOT found. Introduced in EITHER Phase 4's grass-shader `_OrientMode`/orientation changes (`GrassInteractIndirect.shader`) OR Ext5's LOD-to-layer move. Slipped past because the demo GRASS visual was last screenshotted at Phase 2 — afterwards only harnesses ran, and **harnesses verify cull MATH, not the rendered image.** Both CPU+GPU affected → shared cause (LOD-mesh sourcing/scale in the engine render path) more likely than a GPU-only shader path.

## Next Steps
1. **Screenshot the demo grass first** (game_view, `set_active_instance GrassInteract@de203215`) to re-confirm the sparse state — do NOT trust harnesses for the visual.
2. **Bisect the suspect range:** since both tiers regress, inspect the SHARED render input — the LOD mesh + scale the engines feed (GrassCpuEngine/GrassRenderer + GrassGpuEngine) after LOD moved to `ScatterLayer.lods`. Check blade scale/mesh vs pre-refactor. Then check the `GrassInteractIndirect.shader` `_OrientMode < 0.5` default path (rotation/scale composition) from Phase 4.
3. Fast isolation: temporarily revert ONLY the Ext5 LOD-to-layer change and re-screenshot — tells you instantly whether the fault is Phase 4 (shader) or Ext5 (LOD sourcing).
4. After fix: re-screenshot dense grass + re-run all 5 harnesses + verify props/terrain/deform/painter/colliders visually (interactive pass: bind terrain, add a kind=Mesh prop layer with deformMode=On + generateColliders, paint via Inspector, enter Play).

## Git State
- **Not a git repo** (git:false) — no branch/commit/dirty tracking. All changes on-disk only. Verification gate = live-editor evidence, not commits.

## Session Notes
- **LESSON (recorded in memory):** cull-count harnesses do NOT catch shader/LOD RENDER faults. After ANY change to the grass shader or LOD sourcing, screenshot the actual demo grass — don't trust harness PASS for the visual.
- Demo field left `forceTier=Auto` (resolves GPU). `fieldBounds` restored to 40 on the demo layer (had been persisted to 100 by a SaveAssets during migration — a hot-reload-artifact value; disk was 40).
- Full per-step detail (every file, every verification) is in project memory `grassinteract-project-status.md` — newest entries first; the Ext5 entry has the ⚠️ regression with suspects.
- Also this session (pre-plan): per-blade frustum-cull margin fix (separate, shipped+verified) + 2 Unity-kit sync-back PRs (#192 URP, #193/#194 shader-graph).
- New files this plan: Runtime/{ISurfaceSampler, RaycastSurfaceSampler, TerrainSurfaceSampler, ScatterLayer, ScatterField, MeshScatterEngine, ChunkedInstanceBuffer, TerrainScatterConfig}.cs; Shaders/ScatterInstanced.shader; Editor/{ScatterBrush, ScatterFieldEditor, ScatterInstanceCullHarness, ScatterPropMeshBuilder, GrassScatterSamplerVerify}.cs; Assets/GrassInteract/MIGRATION.md; Demo/GrassInteractDemoScatterConfig.asset.

## How to resume / verify
- `set_active_instance GrassInteract@de203215` (port 6403) FIRST.
- Harnesses via execute_code: `GrassInteract.EditorTools.{GrassChunkBakeVerify.Run(16), GrassCullHarness.Run(), GrassBladeCullHarness.Run(), ScatterInstanceCullHarness.Run(), GrassScatterSamplerVerify.Run()}` — all currently PASS.
- execute_code GOTCHA: runs as a method body — NO `using` directives; fully-qualify GrassInteract.*/System.Text.*; `Object` ambiguous → `UnityEngine.Object`.
- MCP reconnect during domain reload is normal — retry `refresh_unity(wait_for_ready)`; never kill the editor.
