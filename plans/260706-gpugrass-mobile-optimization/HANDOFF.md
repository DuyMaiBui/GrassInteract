# Handoff — 2026-07-06 15:30
Generated by: t1k:handoff save
Location: plans/260706-gpugrass-mobile-optimization/HANDOFF.md

## Active Plan
- Path: plans/260706-gpugrass-mobile-optimization/
- Scope: 6 mobile-optimization improvements to the GPUGrass module (`Assets/GPUGrass/`), Unity 6 / URP 17.3, mobile down to GLES 3.0.
- Current phase: **Implementation complete + unit-test gate GREEN.** Remaining: Play-mode render smoke (blocked, see Blockers) → finalize/commit.
- Phase status: blocked (on a one-time Unity focus-click for the render verify only; the code + tests are done)

## Progress

### Completed Tasks
- [x] Wrote `docs/GPUGrass.md` — full architecture + usage reference; §8 = mobile assessment (6 ranked improvements).
- [x] Authored + self-reviewed the plan (`plan.md`), 3 phases.
- [x] **Phase 1** — `enableOcclusionCulling` default → false (`GpuGrassConfig.cs:96`); aggressive `ApplyMobilePreset` (occlusion OFF, LOD {8,20}, cull 60, density 0.5/0.4, shadows off, `_WIND_PERLIN` off, tier Auto) + opaque-shader guardrail (`GpuGrassSceneWindow.cs`).
- [x] **Phase 2** — merged 3 per-LOD `Append` visible-index buffers → 1 packed buffer w/ 2-bit LOD tag (12→4 B/blade, −SSBO bindings). `GpuGrassRenderer.cs`, `GrassCull.compute` (BladeCull → BladeCullCount/WriteLodOffsets/BladeCullScatter), `GpuGrassIndirect.shader` (unpack in all 3 passes), new `GpuGrassLodTag.cs` (pack/unpack SSOT).
- [x] **Phase 3** — trail+interactor deform deleted from DepthOnly + ShadowCaster passes; LOD0-only touch-bend in Forward; `Cull Back` + `_ALPHACLIP`-opt-in confirmed intact. `GpuGrassIndirect.shader`.
- [x] Added 2^30 LOD-tag overflow guard (`GpuGrassRenderer.cs`, review item C).
- [x] Adversarial code review (t1k-code-reviewer): **8/10, no compile-breakers.** Count/scatter parity, prefix-sum offsets, kernel-name parity all verified.
- [x] **Unit tests fixed + VERIFIED GREEN: 57/57** (run via Unity MCP test runner, job `c529afdf...`).
  - `MobilePresetTests.ApplyMobilePreset_WritesAllLockedValues` — fixed by reordering `ApplyMobilePreset`: run `AssetDatabase`-mutating material self-heal FIRST, write config fields LAST, persist immediately. (Root cause: material `CreateAsset` triggered a reimport that reloaded the config from disk, reverting in-memory field writes — a real latent production bug.)
  - `DensityGovernorTests.Headroom_RestoresDensityAfterThinning` — pre-existing timing bug; widened fast-frame window (60→400) past the governor's 0.5 s EMA.
  - `GpuStructStrideTests` (20/24/8/32/48 B) + new `LodTagPackTests` both green.

### In Progress / Not Started
- [ ] **Play-mode render smoke (review item A)** — confirm the grass field renders with the new stride-4 indirect args buffer. Demo scene setup staged (see Next Steps).
- [ ] Finalize: commit on a branch, update `docs/GPUGrass.md` §8 status + plan, sync-back.

## Key Decisions Made
- **#5 + #6 merged into one refactor** — a single packed `Append` buffer (2-bit LOD tag in top bits, `LOD_SHIFT=30`, `INDEX_MASK=0x3FFFFFFF`) solves both the 8 B/blade memory AND the SSBO-binding-count reduction.
- **`ApplyMobilePreset` writes config fields LAST** (after all `AssetDatabase` ops) to dodge the reimport-revert bug. Uses direct SSOT setters (added to `GpuGrassConfig`), NOT a string-keyed `SerializedObject` (which silently no-ops on a missing property).
- **Args buffers reallocated** as `IndirectArguments | Structured`, stride=4, count=5 so `WriteLodOffsets` compute-writes `instanceCount` — this is the ONE thing only a live Play draw can confirm (review item A; blank field = `RenderMeshIndirect` rejecting the stride; fallback = drop `| Structured` or use `Raw`/`ByteAddressBuffer`).

## Blockers
- **Unfocused Unity editor won't recompile new/changed scripts.** `refresh_unity` (force), `reimport_assets`, `Assets/Refresh` menu, and a Play/Stop cycle all failed to trigger the compile of `TempDemoSetupVerify.cs` (one lucky compile landed at 15:06 for the test fixes). Unity's AssetDatabase isn't importing file changes while its window is backgrounded. **Unblock: click once on the Unity Editor window** → it recompiles → then the staged auto-flow runs.
- **Two Unity editors are running** (`GrassInteract@de203215` port 6402 = ours; `UnityTheOneFeatureProject@52452cd9` port 6400). MUST `set_active_instance GrassInteract@de203215` before any MCP test/tool call, or it silently targets the wrong project (early `total:0` runs were this).

## Next Steps
1. **User clicks Unity once** to compile the pending edits (incl. `TempDemoSetupVerify.cs`).
2. Run `Tools ▸ GPUGrass ▸ TempDemoSetupVerify` (creates shared config, assigns the 3 GrassBladeCluster LOD meshes, forces `GrassPlacementSource.TerrainSurface`, bakes all 4 terrains; logs `[TEMP-SETUP]` blade counts + tier). Demo scene `Assets/GPUGrass/Demo/Demo.unity` is already open; its controllers currently have `Config:null`/`Bake:null`/tier Disabled.
3. Enter Play → `rendering_stats get_stats` + a scene screenshot → confirm grass renders (item A). Exit Play.
4. **Delete the temp helper** `Assets/GPUGrass/Editor/TempDemoSetupVerify.cs` (+ .meta).
5. Finalize: commit all GPUGrass changes on a branch (pathspec — do NOT sweep the session-start `.claude/` kit-update files), update docs §8 + plan status, then sync-back.

## Git State
- Branch: `main` (⚠️ need a feature branch before committing)
- Last commit: `7b27604 chore: commit working-tree changes (WorldPainter, GPUGrass demo, project assets, plans)`
- Dirty (mine): `GpuGrassSceneWindow.cs`, `GpuGrassConfig.cs`, `GpuGrassRenderer.cs`, `GpuGrassIndirect.shader`, `GrassCull.compute`, `DensityGovernorTests.cs` (modified); new: `AssemblyInfo.cs` (InternalsVisibleTo for tests), `GpuGrassLodTag.cs`, `LodTagPackTests.cs`, `MobilePresetTests.cs`, `docs/GPUGrass.md`, `plans/260706-gpugrass-mobile-optimization/`, **`TempDemoSetupVerify.cs` (DELETE before commit)**.
- Also dirty but NOT mine: a large set of `.claude/**` files from the session-start `t1k modules update` — do NOT stage these.
- Uncommitted: nothing committed this session (holding for green suite + render verify).

## Session Notes
- The MobilePreset debugging burned many cycles because the unfocused editor served STALE DLLs — early "still failing" results were against uncompiled code, and one run silently hit the wrong project (port 6400). Lesson: always verify the compiled DLL contains the edit (`strings <dll> | grep`) and `set_active_instance` before trusting a test result.
- `execute_code` MCP tool is BROKEN in this env (mono "filename too long" / CodeDom) — do not use it; use a temp `[MenuItem]` script instead.
- Benign console noise during EditMode runs: `Assertion failed: targetScene != nullptr` (test-framework undo/cleanup for tests doing scene/asset I/O — review item E). Not a failure; suite is 57/57.
